"""Evaluation library — golden-set scoring and report generation.

Golden data and CLI entrypoints live under ``backend/evals/`` and
``backend/scripts/eval_*.py``. Import scoring helpers from submodules here:

- ``app.evals.rag`` — retrieval / chunking / structured lookup eval
- ``app.evals.tutor_board`` — tutor card (AiBoard) eval
- ``app.evals.dashboard`` — frontend report path helpers
"""
